name = "contribute"
description = "Suggest features or contribute code to the moxxy project via GitHub issues and pull requests."
version = "v1.0.0"
needs_network = true
needs_fs_read = true
needs_fs_write = true
needs_env = true
run_command = "bash"
entrypoint = "run.sh"
