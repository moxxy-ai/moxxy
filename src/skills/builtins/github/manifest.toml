name = "github"
description = "Interact with any GitHub repository - create issues, fork repos, open pull requests, and more."
version = "v1.0.0"
needs_network = true
needs_fs_read = true
needs_fs_write = true
needs_env = true
run_command = "bash"
entrypoint = "run.sh"
