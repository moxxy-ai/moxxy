name = "github"
description = "Interact with GitHub: create issues, open pull requests, fork repos, clone, comment, and list issues/PRs."
version = "v1.0.0"
needs_network = true
needs_fs_read = true
needs_fs_write = true
needs_env = true
run_command = "bash"
entrypoint = "run.sh"
