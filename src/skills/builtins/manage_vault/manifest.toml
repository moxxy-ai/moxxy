name = "manage_vault"
description = "Manage vault secrets: list keys, get value (instruction: mask it), set or remove secrets."
version = "1.0.0"
needs_network = true
needs_fs_read = false
needs_fs_write = false
needs_env = true
