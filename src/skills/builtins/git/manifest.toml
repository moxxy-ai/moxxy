name = "git"
description = "Execute git commands in the agent's workspace."
version = "v1.0.0"
needs_network = true
needs_fs_read = true
needs_fs_write = true
needs_env = true
run_command = "sh"
entrypoint = "run.sh"
