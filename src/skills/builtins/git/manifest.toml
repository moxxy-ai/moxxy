name = "git"
description = "Execute git commands and interact with GitHub repositories - create issues, fork repos, open pull requests, and more."
version = "v2.0.0"
needs_network = true
needs_fs_read = true
needs_fs_write = true
needs_env = true
run_command = "bash"
entrypoint = "run.sh"
