# Git Skill

Use this skill to execute git commands in the agent's workspace.

## Usage
Provide the git arguments to execute.

```bash
git status
git commit -m "Initial commit"
```

If `git` is not installed on the system, the skill will return an error message prompting you to install it.
