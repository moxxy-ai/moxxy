# Host Shell Proxy Skill

**RESTRICTED** - Only use when the user explicitly asks to run something on their machine. Never use autonomously. Always prefer dedicated skills (git, github, etc.).

Executes bash commands natively on the host OS via the Host Proxy.

`<invoke name="host_shell">["ls -la /tmp"]</invoke>`
