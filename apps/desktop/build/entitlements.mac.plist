<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Entitlements for the macOS Hardened Runtime (required for notarization).
  Electron / V8 JIT-compile JavaScript, so the runtime must allow JIT and
  unsigned executable memory or the app is killed at launch. Only applied when
  the build is actually signed with a Developer ID (see docs/desktop-code-signing.md).
-->
<plist version="1.0">
  <dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Microphone access for voice input (speech-to-text). Without this a
         hardened-runtime signed build is denied the mic and recordings come
         back silent. Pairs with NSMicrophoneUsageDescription in package.json. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
  </dict>
</plist>
