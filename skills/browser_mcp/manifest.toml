name = "browser_mcp"
description = "MCP-backed browser skill that proxies selected chrome-devtools MCP tools (initial skeleton)."
version = "v0.1.0"
needs_fs_read = true
needs_fs_write = true
needs_env = true
run_command = "bash"
entrypoint = "run.sh"
