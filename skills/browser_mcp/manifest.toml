name = "browser_mcp"
description = "Browser skill using an external MCP chrome-devtools server for fetch, navigate, and screenshot actions. Uses a configured MCP server (see README)"
version = "v0.1.0"
needs_env = true
run_command = "bash"
entrypoint = "run.sh"
