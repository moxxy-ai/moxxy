diff --git a/dist/types/icns.js b/dist/types/icns.js
index f2bfafef3723cb423b110304815e56e3f97f81e0..29e6bc663a17ccac84b2ea50305d153efe7049b9 100644
--- a/dist/types/icns.js
+++ b/dist/types/icns.js
@@ -64,11 +64,18 @@ const ICON_TYPE_SIZE = {
     ic10: 1024,
 };
 function readImageHeader(input, imageOffset) {
+    if (imageOffset + SIZE_HEADER > input.length) {
+        throw new TypeError('Invalid ICNS entry header');
+    }
     const imageLengthOffset = imageOffset + ENTRY_LENGTH_OFFSET;
-    return [
+    const header = [
         (0, utils_1.toUTF8String)(input, imageOffset, imageLengthOffset),
         (0, utils_1.readUInt32BE)(input, imageLengthOffset),
     ];
+    if (header[1] < SIZE_HEADER) {
+        throw new TypeError('Invalid ICNS entry length');
+    }
+    return header;
 }
 function getImageSize(type) {
     const size = ICON_TYPE_SIZE[type];
@@ -81,6 +88,9 @@ exports.ICNS = {
         const fileLength = (0, utils_1.readUInt32BE)(input, FILE_LENGTH_OFFSET);
         let imageOffset = SIZE_HEADER;
         let imageHeader = readImageHeader(input, imageOffset);
+        if (imageOffset + imageHeader[1] > fileLength || imageOffset + imageHeader[1] > inputLength) {
+            throw new TypeError('Invalid ICNS entry bounds');
+        }
         let imageSize = getImageSize(imageHeader[0]);
         imageOffset += imageHeader[1];
         if (imageOffset === fileLength)
@@ -92,6 +102,9 @@ exports.ICNS = {
         };
         while (imageOffset < fileLength && imageOffset < inputLength) {
             imageHeader = readImageHeader(input, imageOffset);
+            if (imageOffset + imageHeader[1] > fileLength || imageOffset + imageHeader[1] > inputLength) {
+                throw new TypeError('Invalid ICNS entry bounds');
+            }
             imageSize = getImageSize(imageHeader[0]);
             imageOffset += imageHeader[1];
             result.images.push(imageSize);
diff --git a/dist/types/jxl.js b/dist/types/jxl.js
index f557b0aea7e679d2cb84d9915e957dd2254eba97..28428374a2fbe05212eeb06a77e7540ebe5e7b70 100644
--- a/dist/types/jxl.js
+++ b/dist/types/jxl.js
@@ -23,6 +23,9 @@ function extractPartialStreams(input) {
         const jxlpBox = (0, utils_1.findBox)(input, 'jxlp', offset);
         if (!jxlpBox)
             break;
+        if (jxlpBox.size < 12) {
+            throw new TypeError('Invalid JXL partial codestream box');
+        }
         partialStreams.push(input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size));
         offset = jxlpBox.offset + jxlpBox.size;
     }
