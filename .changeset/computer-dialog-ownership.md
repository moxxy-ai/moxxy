---
'@moxxy/cli': patch
'@moxxy/desktop': patch
---

Keep Windows dialog controls bound to their actual window, expose modal ownership, and return a blocking dialog instead of waiting for focus on its disabled parent. Version the native helper contract together with the extension.
