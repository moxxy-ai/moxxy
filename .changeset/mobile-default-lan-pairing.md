---
"@moxxy/cli": patch
---

Make `moxxy mobile` phone-friendly by default: bind the mobile gateway on LAN, advertise the reachable Wi-Fi/hotspot IP in the QR, and keep loopback pairing as an explicit simulator/local-only opt-in.
