---
'@moxxy/desktop': patch
---

Paint Focus Mode with the current Moxxy mark. The floating widget rendered a raster mascot, the mini-text header rendered the superseded app icon, and the pre-mount boot tile rendered a typed `m` glyph. All three now draw the woven mark: the widget turns it clockwise and blooms it with the voice level, sharing Voice Mode's own spin, phase-speed and energy helpers, so the floating twin and the full sculpture read as one object.
